const TILE_SIZE = 32;

const TILES_WIDTH = 20;
const TILES_HEIGHT = 20;

const WORLD_WIDTH = TILES_WIDTH * TILE_SIZE;
const WORLD_HEIGHT = TILES_HEIGHT * TILE_SIZE;

const BODY_WIDTH = TILE_SIZE;
const BODY_HEIGHT = TILE_SIZE;

const UNIT_SKINS = 6;
const UNIT_SPEED = 140;

export {
    TILE_SIZE,
    TILES_WIDTH,
    TILES_HEIGHT,
    WORLD_WIDTH,
    WORLD_HEIGHT,
    BODY_WIDTH,
    BODY_HEIGHT,
    UNIT_SKINS,
    UNIT_SPEED
}
