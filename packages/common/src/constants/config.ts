const INTERPOLATE_MS = 100;

const SIMULATION_RATE = 20;
const SNAPSHOT_RATE = -1;

const FRACTION_DIGITS = 3;

export {
    INTERPOLATE_MS,
    SIMULATION_RATE,
    SNAPSHOT_RATE,
    FRACTION_DIGITS
};
